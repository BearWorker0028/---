# 裕珍皇專案 - 今日待辦任務與架構推進計畫

## 📅 開發階段推進原則

依據核心原則：**「核心業務功能先行完備 ➔ 驗證無誤 ➔ 一併封裝 地端正式版 (SQLite+Modbus) 與 遠端開發版」**。

---

## 📌 今日已完成項目 (Done)

- [x] **點位定義更新與精簡**：
  - 取消 2 項硬體保護點位（Bit 9、Bit 10），正名復原 2 項核心設備異常（**L212 設備異常**、**L216 設備異常**）。
- [x] **啟動流程簡化**：`start.bat` 一鍵啟動 Web 服務與即時橋接器。
- [x] **【功能 1】警報查詢與生命週期管理**：
  - 頂部 3 大精簡 KPI 卡片（`🚨 當前未解除`、`🔴 本月高溫警報`、`🟠 本月設備異常`）。
  - 二行式左對齊固定工具列（純化名稱、日期區間、即時查詢、Excel 匯出）。
  - 自動計算警報持續秒數、精確標記復歸時間。
- [x] **【功能 2】LINE 官方帳號 Bot 即時推播（金煜 Flex 卡片同款）**：
  - 採用 **LINE Messaging API Push** 主動推播（日常運作 100% 免 Webhook / 免固定 IP）。
  - **完全沿用金煜專案同款 Flex Message（Bubble 視覺化彩色卡片模板）**：
    - 🔴 高溫警報 (`#FF3B30`) / 🔵 低溫警報 (`#007AFF`)
    - 🟠 L212 / L216 設備異常（統一暖橘紅 `#F97316`，層級比純紅降階）
    - ⚠️ RS-485 網關通信異常（琥珀黃 `#D97706`，方案 A 根因遮蔽合併單則不洗版）
    - 🟢 溫度恢復正常通知（草綠色 `#34C759`，動態門檻調整即時連動復歸）
- [x] **【功能 3】電視牆螢幕解析度設定與卡片/平面圖滿版自適應引擎**：
  - 支援 6 種主流解析度（`auto`、`1920x1080`、`3840x2160`、`2560x1440`、`1366x768`、`1280x720`）。
  - 卡片視圖與平面圖視圖滿版等比縮放，雙向記憶案場設定。

---

## 📋 核心任務清單 (Consolidated Tasks)

### 🎯 任務 1：【報表下載】模組與 冷鏈庫溫歷程 / SPM-3 能源統計報表引擎 (已完成 ✅)
- [x] **前端「報表下載」完整查詢面板與多維度篩選工具列**：
  - 頂部 4 大報表 KPI 動態統計卡片（全期均溫/總耗電、最高溫/最大功率、最低溫/平均功率、合格率/功率因數）。
  - 二行靠左排列固定工具列（報表類型、監控對象、查詢週期、採樣頻率、自訂日期區間）。
  - 報表類型：`冷鏈庫溫歷程紀錄表` / `SPM-3 電表能源統計報表`。
  - 監控對象：一庫一表獨立產出（6 大庫別獨立選擇；電表支援 `全廠電表資訊`、`1F 集合式電錶`、`3F 集合式電錶`）。
  - 查詢週期：自動向前推算完整週期（`日報表 (前一日)`、`週報表 (前一週)`、`月報表 (前一月)`、`自訂區間`）。
  - 採樣頻率：`5分鐘`、`10分鐘`、`15分鐘`、`30分鐘`、`1小時`、`1分鐘`（附客觀資料筆數技術參考）。
  - 頁面即時表格數據預覽（包含序號、採樣時間、溫度/電力數據、合格狀態標籤與超標高亮）。
- [x] **冷鏈庫溫歷程 Excel 生成引擎 (`/api/reports/export?category=temp`)**：
  - 標準工程監控表頭（廠區、庫別、區間、門檻基準、合格率、最高/最低/平均溫、超標次數與偏離時長）。
  - 依選定採樣間隔智能分桶聚合，格式化藍底白字表頭與斑馬紋明細。
- [x] **SPM-3 能源報表 Excel 生成引擎 (`/api/reports/export?category=energy`)**：
  - 支援全廠合併或單電表報表，包含累積用電量 (kWh)、瞬時功率 (kW)、平均線電壓 (V)、三相電流 (A) 與夏月夜尖峰時段分類標籤。

---

### 🎯 任務 2：設備控制指令下發 (目標庫溫 40007 + 開關機 40001/40179)
- [ ] 控制面板 Modal 增加「目標庫溫設定 (40007)」與「主機開關機/停控 (40001/40179)」操作按鈕。
- [ ] 指令下發至 `device_commands` 佇列，地端 Modbus 輪詢執行寫入並回報執行狀態。
- [ ] 介面即時防呆與指令下發成功/失敗提示 Toast。

---

### 🎯 任務 3：地端正式版 (SQLite WAL + Modbus TCP) 封裝與正式交付
- [ ] **地端 SQLite 輕量化儲存引擎定案**：
  - 啟用 WAL 模式（斷電防損毀、免裝 PostgreSQL/Docker、免年費）。
  - 季度自動分庫與查詢索引。
- [ ] **正式環境執行腳本打包**：
  - 地端案場直連 Modbus (W610 @ 192.168.68.200:2000)。
  - 非同步 Supabase 雲端即時同步（供總部遠端看盤）。
  - 開機自動載入、靜默後台常駐。
